import java.util.HashMap;

public class PassingCars {
    public int passingCarPairs(int[] A) {
        int pairNumber = 0;
        if (A.length == 0) {
            return pairNumber;
        }

        int firstElement = A[0];
        HashMap<Integer, Integer> pairMap = new HashMap<>();
        for (int i = 1; i < A.length; i++) {

        }
        return pairNumber;
    }
}
